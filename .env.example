# Copie este arquivo para `.env` e preencha com as credenciais do Supabase.
# São as mesmas usadas pelo Portal de BI e pelo Hub (mesmo projeto/usuários).
# Ambas são públicas por design (vão embutidas no bundle) e protegidas por RLS.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
